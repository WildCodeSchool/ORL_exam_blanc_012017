{% extends 'base.html.twig' %}

{% block body %}
    <h1>Requette</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ requette.id }}</td>
            </tr>
            <tr>
                <th>Nb</th>
                <td>{{ requette.nb }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('requette_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('requette_edit', { 'id': requette.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
